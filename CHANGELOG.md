# Changelog

本项目从 `2.2.0` 开始按标准 SemVer、独立发布元数据提交和不可变 annotated tag 记录变更。

## [2.11.4] - 2026-08-20

- 修复归档排序图标在紧凑尺寸下左上笔画显示不完整的问题；排序图标恢复为与通用
  工具栏一致的 18px 尺寸并允许 SVG 笔画完整显示，永久删除图标和按钮外框保持不变。

## [2.11.3] - 2026-08-20

- 统一普通与归档任务的卡片结构、来源元数据、选择状态和操作槽位置；所有卡片固定显示
  “任务类型 · 象限”，选择框、删除、恢复及归档图标保持一致尺寸和居中对齐。
- 重整归档操作区：按“排序、永久删除、取消、选择/全选”排列，已选数量统一移至共享
  footer，并支持单条恢复、单条永久删除及批量永久删除。
- 完善搜索、归档、任务类型、任务卡片和象限之间的四区键盘导航，补齐空任务列表的
  左右切换，并修复离开归档时搜索框短暂闪现旧焦点边框的问题。
- 为“🌐 全部象限”补充统一图标，并保持象限纵向导航在顶部和底部边界停止。
- 活动任务统一按修改时间优先、创建时间兜底的规则倒序排列；新建或修改的任务会跨
  任务类型和象限置顶，同时间任务保持稳定顺序，归档排序逻辑不变。

## [2.11.3-beta.6] - 2026-08-20

- 修复空任务列表的横向键盘导航：当任务区只剩新增入口时，按右方向键进入当前象限，
  再按左方向键返回新增入口，不再因单个控件原地循环而失效。

## [2.11.3-beta.5] - 2026-08-20

- 完善四区空间键盘导航：搜索或归档可用向下键进入当前任务类型，任务类型可用向上键
  返回原工具控件，新增任务入口可用向上键返回当前任务类型；任务卡与象限之间支持
  右/左键往返，并在重绘后恢复原任务焦点。
- 象限纵向导航在顶部“🌐 全部象限”和底部新增入口处停止，不再首尾循环；“全部象限”
  增加地球 emoji，与其他象限入口的图标结构保持一致。
- 修复从归档入口用左方向键切回搜索时，旧搜索控件短暂获得白色焦点边框后又被重绘
  替换的问题；现在等待普通任务视图重建后再聚焦真实搜索控件。

## [2.11.3-beta.4] - 2026-08-20

- 统一普通与归档任务卡片的选择框和删除图标：两者共用 18px 居中操作槽与 16px
  内部图形，并覆盖 Obsidian 的复选框默认边距，避免选择状态下出现水平偏移。

## [2.11.3-beta.3] - 2026-08-20

- 所有普通与归档任务卡片固定显示“任务类型 · 象限”来源，不再随筛选范围省略，
  避免切换全部任务、具体任务类型、象限或归档时卡片高度变化。

## [2.11.3-beta.2] - 2026-08-20

- 统一归档与普通任务工具栏中“取消”和“选择/全选”的图标尺寸；两种模式继续共享
  33px 按钮外框和 18px 图标，仅归档排序与永久删除保留紧凑图标。

## [2.11.3-beta.1] - 2026-08-20

- 统一归档与普通任务的选择交互：归档顶部按“排序、永久删除、取消、选择/全选”排列，
  已选数量改在共享 footer 中显示，筛选范围变化时自动清除旧选择。
- 抽离普通与归档任务共用的卡片元数据行和选择按钮组件，使不同任务类型、象限及
  归档范围中的正文、来源、时间和操作位置保持一致。
- 为单条归档任务补充与普通卡片操作区对齐的恢复和永久删除按钮，并保留批量永久
  删除、键盘焦点和运行数据保护语义。

## [2.11.2] - 2026-08-19

- 修复命令面板与动态任务类型脱节的问题：新增、重命名和删除任务类型后，聚焦命令
  会同步更新，同时继续保留 `focus-work`、`focus-personal` 历史命令 ID。
- 加固持久化数据兼容边界：历史 `schemaVersion` 安全归一到当前版本；非法值或未来
  版本停止初始化，避免旧插件静默覆盖不兼容数据。
- 明确 JSON 备份版本合同，支持无版本历史备份、`2.0` 和 `4.0`；未来版本、未知版本、
  声明与结构不匹配或重复 ID 的备份会在写入前完整拒绝。
- 收紧 Store 状态所有权，传入数据和根状态读取不再共享嵌套可写引用；无效任务类型
  切换不会重置当前查询范围。
- 将归档卡片的元数据与恢复/勾选操作统一到底部操作区；进入归档时清除普通任务多选，
  避免两个模式同时保留选择状态。

## [2.11.1] - 2026-08-19

- 将普通任务卡片的多选框移至右下角原删除按钮位置，并取消标题区域的额外右侧
  留白，避免进入多选状态后长标题提前换行。

## [2.11.0] - 2026-08-19

- 新增任务卡右键移动、桌面拖拽移动和多选批量移动，支持分别或同时更换任务类型与
  象限；整批移动采用完整校验与原子写入，并同步刷新所有受影响的 Markdown 笔记。
- 支持拖拽重新排列任务类型和象限，使用完整占位槽和数量感知拖拽影像，并提高相邻
  无效位置及目标中点附近的稳定性。
- 重构任务多选与新增任务布局：选择操作固定在任务区顶部，已选数量移至 footer；
  打开新增任务时自动退出多选，折叠入口、单行输入框和任务卡片保持稳定高度。
- 完善看板键盘导航：任务类型向下或按 Tab 时先进入新增任务入口，任务区顶部按钮
  支持左右循环并跳过禁用项；选择重绘后继续保留焦点，可用上下方向键切换卡片并用
  Enter 反复选中或取消选中。已选卡片继续显示紫色状态，当前键盘焦点额外显示白边。

## [2.11.0-beta.11] - 2026-08-18

- 为新增任务“+”补充与任务卡等键盘目标一致的白色焦点边框，明确显示 Tab 和
  方向键当前所在位置。
- 修复进入多选、全选或切换单张卡片后因重绘丢失键盘焦点的问题；选择模式下可用
  上下方向键继续切换卡片，并用 Enter 反复选中或取消选中。

## [2.11.0-beta.10] - 2026-08-18

- 修复任务类型与任务区之间的键盘焦点顺序：按 Tab 或从任务类型按向下方向键时先
  进入新增任务“+”，不再直接落到首张任务卡。
- 支持在新增任务、“关闭选择”和“多选”按钮之间使用左右方向键循环；“关闭选择”
  不可用时会自动跳过。
- 提高任务类型与象限重排在目标中点附近的稳定性，避免轻微指针抖动导致占位槽
  反复切换或消失。

## [2.11.0-beta.9] - 2026-08-18

- 统一新增任务入口与单行输入框的占用高度，避免展开输入框时任务卡片发生垂直
  位移；输入多行内容后仍可自动增高。

## [2.11.0-beta.8] - 2026-08-18

- 新增任务输入框展开时隐藏“关闭选择”和“多选”按钮，并禁止其响应点击；输入框
  提交、取消或失焦收起后自动恢复按钮。

## [2.11.0-beta.7] - 2026-08-18

- 修复任务类型与象限拖拽经过同一插入位置时占位标志反复消失、出现的问题，使
  占位槽在 WebKit 布局变化期间保持稳定。
- 打开新增任务输入框时自动退出多选并清空已选卡片，避免新增任务与批量操作状态
  同时存在。
- 将“已选 X 项”移到看板 footer 左侧，避免被 Obsidian 右下角原生状态栏遮挡。

## [2.11.0-beta.6] - 2026-08-17

- 统一任务卡片在选择与非选择状态下的高度，并将新增任务入口、目标选择、选择操作
  与“全部象限”统一为相同控件高度；展开后的任务输入框与任务卡片同宽。
- 任务类型和象限拖拽回到自身或相邻无效位置时不再显示占位标志，也不会提交无效
  重排。

## [2.11.0-beta.5] - 2026-08-17

- 顶部任务类型和右侧象限排序改用完整的虚线占位槽与位于光标上方的紧凑拖拽
  影像，使插入位置不再被拖动项遮挡。
- 新增固定的看板底部提示区，并将多选状态的“已选 X 项”从任务列表顶部移到底部
  显示，为后续筛选、同步等状态提示预留统一位置。

## [2.11.0-beta.4] - 2026-08-17

- 移除多选任务工具栏中的独立移动按钮；将始终显示的“关闭选择”和“多选”按钮移至
  新增任务按钮右侧，其中“关闭选择”在未进入选择状态时禁用。批量移动继续通过拖拽
  或右键任务卡片完成。
- 将任务拖拽起点限制在正文下方的来源、时间和操作区域；正文保持文本光标并禁止从
  此处开始拖拽。
- 支持拖拽重新排列顶部任务类型和右侧象限，并与任务卡片拖到类型或象限的移动操作
  保持独立。

## [2.11.0-beta.3] - 2026-08-17

- 多选任务拖拽时使用包含任务数量的拖拽影像，明确显示实际批量移动数量。
- 将普通任务卡片的多选框移至右上角，避免占用任务标题左侧空间。

## [2.11.0-beta.2] - 2026-08-17

- 将任务类型拖拽目标的外扩阴影改为完整白色实线边框，避免横向容器裁剪后在目标
  右侧出现半圆阴影。

## [2.11.0-beta.1] - 2026-08-17

- 将 `aulyc-dual-mirror-v1` 项目采用与中央渠道登记同步升级到 `1.7.0`；新增的
  macOS 简洁附件规则对本 Obsidian 插件为 `N/A`，Community 三文件合同保持不变。
- 新增任务卡右键移动、桌面拖拽移动和多选批量移动，可分别或同时更换任务类型与
  象限；跨任务类型移动后会同步刷新全部受影响的 Markdown 笔记。
- 批量移动采用整批校验与原子写入，目标无效、任务重复或 ID 冲突时不会产生部分
  移动。

## [2.10.0] - 2026-08-04

- 新增“界面语言”设置，支持跟随 Obsidian、简体中文和 English；语言切换不改写
  已有任务、象限或同步目录。
- 将插件清单介绍改为固定的中英双语短句。

## [2.9.7] - 2026-08-04

- 将设置页“同步文件夹”改为 Obsidian 原生下拉框，只列出 Vault 顶层文件夹并禁止
  手动输入；旧配置值不在当前文件夹列表时仍作为兼容选项保留。
- 移除自定义紫色勾选、强调边和文件夹建议弹层，跟随 Obsidian 原生选择控件样式。

## [2.9.6] - 2026-08-03

- 优化设置页“同步文件夹”的可编辑选择框样式：已有值时显示选中强调、勾选图标和
  下拉提示，同时保留直接输入、文件夹建议和键盘焦点反馈。

## [2.9.5] - 2026-08-03

- 接入 Obsidian 官方 ESLint 规则和当前 API 类型，补充声明式设置定义，并将计时器、
  DOM 创建、活动元素及窗口访问收敛到对应 Vault/Workspace 上下文。
- 移除 Community 自动审查指出的 `!important`、`:has()` 及其他可替代 CSS 写法，
  通过显式焦点状态保持任务类型、象限和输入控件的原有交互效果。
- GitHub 主仓与 Gitee 镜像的 Community 发布附件精确限定为 `main.js`、
  `manifest.json` 和 `styles.css`；ZIP、校验和及 provenance 继续作为本地验证证据。

## [2.9.4] - 2026-08-02

- 改用与最低版本 `1.5.0` 兼容的公开 Vault/Workspace API，修复 Community
  自动审查的最低版本阻断项，同时保持文件夹建议与看板激活行为。
- 使用 Obsidian 的 CSS helper 更新任务编辑框高度，并从当前 Workspace 文档读取
  语言，修复静态样式赋值阻断项并改善弹出窗口兼容性。

## [2.9.3] - 2026-08-02

- 为 Obsidian Community 页面补充完整英文 README 概览、功能、安装和隐私说明。
- 移除只读 `localStorage` 语言回退；语言无法从 Obsidian 配置或文档语言取得时直接
  使用英文，避免社区审查把它误判为插件数据持久化。

## [2.9.2] - 2026-08-02

- 将 GitHub 源码、GitHub Release 与 Obsidian Community canonical repository
  统一为 `aulyc/aulycKanban`，保留 Gitee Release 作为第二公开分发源。
- 正式双源发布为 Community 渠道补充独立 `main.js`、`manifest.json` 和
  `styles.css` 附件，并继续发布 ZIP、校验和、provenance 与 `latest.json`。
- 移除项目内更新设置、启动检查、手动检查命令和更新弹窗；社区收录后由
  Obsidian 官方更新机制直接消费 GitHub Release。
- 将设置数据 schema 升级到 8，加载旧数据时安全移除已废弃的
  `autoCheckUpdates` 偏好，不改变看板、同步配置或用户任务数据。

## [2.9.1] - 2026-08-02

- 将 `aulyc-dual-mirror-v1` 采用与中央渠道登记同步升级到 `1.4.0`。
- 明确数字签名对当前“检查更新并打开下载页面”流程为 `N/A`，未来新增应用内安装器时再作为独立安全门禁评估。

## [2.9.0] - 2026-08-01

- 新增应用内正式更新检查：GitHub 优先、Gitee 回退，自动检查默认关闭；发现新版本后由用户明确打开官方下载页面，插件不下载、安装、重载或替换任何插件文件。
- 将数字签名登记为未来应用内下载/安装能力的安全增强，不作为当前测试或正式发版的前置条件、门禁或阻断项。

## [2.8.2] - 2026-07-31

- 升级开发依赖并调整 ESLint 配置，修复依赖安全公告，使 `npm audit` 恢复为零漏洞；开发环境最低 Node.js 版本调整为 20.19。
- 拆分纯测试/正式发版与显式安装入口，确保纯发版不写入 Vault；正式发版安装继续使用精确标签产物并执行真实加载验证。
- 对齐中央发布规范 core 3.0.0、Obsidian 插件 Profile 2.0.0 及双镜像策略操作语义。

## [2.8.1] - 2026-07-26

- 优化归档浏览与批量选择工具栏，统一排序、选择、全选和删除操作的顺序、对齐与状态反馈。
- 区分任务、任务类型、象限和归档入口的业务选中、键盘焦点与鼠标悬停状态，修复归档焦点恢复和选择模式交互。
- 将任务卡调整为单击选择、双击内容或按 Enter 编辑；已应用的搜索标签现在也支持双击原位修改、确认、取消或清除。
- 新增“关于 aulycKanban”信息页，统一设置页操作按钮的文案、宽度与指针反馈，并修正同步文件夹输入框的内描边。
- 加固备份导入的重复 ID 校验和受管笔记同步边界，补齐插件生命周期及 UI 源码覆盖率门禁。

## [2.8.1-beta.12] - 2026-07-22

- 增强未选中任务类型的鼠标悬停背景与边框反馈，使其在深色主题下更容易辨认。
- 为设置页可用操作按钮补充指针光标，禁用按钮保持原有状态。

## [2.8.1-beta.11] - 2026-07-22

- 为“全部象限”及当前选中的具体象限补充鼠标悬停高亮，同时保留原有键盘焦点边框。

## [2.8.1-beta.10] - 2026-07-22

- 将设置页“立即同步”按钮精简为“同步”，并统一备份、导入、清除、同步与关于按钮的宽度。

## [2.8.1-beta.9] - 2026-07-22

- 备份导入拒绝重复的任务类型、象限或任务 ID，避免静默丢失及误编辑；任务正文包含同步结束标记时不再反复生成历史副本，并为插件入口补齐生命周期覆盖率门禁。

## [2.8.1-beta.8] - 2026-07-22

- 在插件设置页新增“关于 aulycKanban”信息页，展示当前版本、Obsidian 版本要求、应用说明、官网与致谢。
- 将同步文件夹输入框的聚焦描边改为向内增加 1px，避免边框向控件外侧扩张。

## [2.8.1-beta.7] - 2026-07-22

- 将普通任务卡调整为单击只选择、双击任务内容或按 Enter 进入编辑，避免一次点击连续触发选择和编辑。

## [2.8.1-beta.6] - 2026-07-22

- 移除归档列表容器的整区白色焦点框；非空归档聚焦第一张任务卡片，空归档聚焦局部排序按钮。
- 统一归档卡片右上角恢复与勾选控件的居中槽位；选中卡片保持原有背景，仅使用红色边框提示。

## [2.8.1-beta.5] - 2026-07-21

- 修复归档选择模式取消全选后按钮被禁用、无法再次全选的问题，并保持删除按钮在零选中时不可用。
- 恢复普通任务卡日期与时间的单行显示，来源信息仍单独占一行。

## [2.8.1-beta.4] - 2026-07-21

- 区分归档入口的未激活与激活视觉状态：未激活时保持透明背景，进入归档后显示橙色填充和白色边框。

## [2.8.1-beta.3] - 2026-07-21

- 统一归档浏览与选择状态的操作顺序，将排序、选择、全选/取消全选和删除改为连续图标按钮，并为已选数量预留三位数字宽度。
- 将归档任务的勾选控件移到卡片右上角，与浏览状态的恢复按钮保持相同位置。

## [2.8.1-beta.2] - 2026-07-21

- 将归档未选择状态的选择和排序按钮移到左侧，并与选择状态下的取消选择和排序按钮逐列对齐。

## [2.8.1-beta.1] - 2026-07-21

- 调整归档批量选择工具栏布局，将取消选择和排序操作移到左侧、全选和已选数量移到右侧，并保持删除操作独立。

## [2.8.0] - 2026-07-21

- 新增“全部任务”和“全部象限”聚合范围、独立搜索/归档工具行，以及工具、任务类型、任务内容和象限四区域键盘循环。
- 完善归档任务类型与象限交叉筛选、正序/倒序图标、批量选择操作，并修复通过 Tab 切换后任务分类与列表状态不一致的问题。
- 统一新增入口和导航焦点样式，修复象限鼠标选择后背景变色及重渲染丢失白色焦点边框的问题，同时固定任务日期与时间分行显示。
- 加固备份导入和同步目录校验，启用 TypeScript strict、分层覆盖率门禁及可配置的中央正式发版流程。

## [2.8.0-beta.9] - 2026-07-21

- 修复通过 Tab 离开归档后旧归档按钮焦点被错误恢复，造成任务类型已切换但任务列表仍显示归档状态的问题。
- 归档排序使用不同图标区分最新优先和最早优先，并在选择模式中保留同位置排序入口，将删除、取消选择和排序统一为右侧图标操作。

## [2.8.0-beta.8] - 2026-07-20

- 恢复固定的“全部象限”入口、汇总数量和键盘导航，并在聚合范围及归档模式中保持正确选中状态。
- 将归档工具栏的选择和批量删除改为图标按钮，排序改为上下双箭头图标，并把选择与排序操作按顺序右对齐。

## [2.8.0-beta.7] - 2026-07-20

- 将任务类型、任务列表和象限区新增按钮的键盘焦点统一为悬停样式，避免 Tab 导航时出现整块紫色和白色实线框。

## [2.8.0-beta.6] - 2026-07-20

- 修正指定 Vault 时的 Obsidian CLI 参数顺序，确保自动发现、插件重载和真实加载 smoke 能连接目标 Vault。
- 修复添加任务按钮与输入框高度不一致导致空任务提示在切换时上下跳动。
- 归档内容改为仅在归档按钮持有键盘焦点时临时显示；通过 Tab、Shift+Tab、左右键或 `Command+F` 离开后恢复普通任务列表。

## [2.8.0-beta.5] - 2026-07-20

- 修正 Obsidian 在文档捕获阶段提前接管 `Command+F`，导致已安装看板无法聚焦搜索框的问题。

## [2.8.0-beta.4] - 2026-07-20

- 看板内新增 `Command+F` 搜索快捷键，可直接聚焦搜索输入框或已有搜索标签，不改变当前任务范围。
- 归档按钮获得键盘焦点时立即进入归档视图，并按进入前选择的具体任务类型（或全部任务）与当前象限交叉筛选归档内容，同时保留对应任务类型的选中状态。

## [2.8.0-beta.3] - 2026-07-20

- 工具区的搜索与归档改用左右方向键切换焦点，上下方向键不再接管；搜索框已有文字时保留原生光标移动，Tab 仍只切换区域焦点而不自动激活归档。

## [2.8.0-beta.2] - 2026-07-20

- 修正四区键盘循环进入工具区时的默认焦点，使 Tab 直接落到归档文件夹；恢复任务类型栏的“全部任务”入口及其左右方向键导航。

## [2.8.0-beta.1] - 2026-07-20

- 备份导入新增逐任务深层校验，历史损坏任务会安全清洗，渲染层同时增加缺失内容兜底。
- 同步目录拒绝 `..` 路径段，避免受管 Markdown 逃出 Vault；中央正式发版脚本改由 `AULYC_STANDARDS_ROOT` 可配置定位。
- 项目采用的公共 core 升级到 `2.0.1`，固定 Obsidian/TypeScript/Node 类型依赖，启用 TypeScript strict，并新增可归因到 `src/` 的分层覆盖率门禁。
- 任务卡元数据中的日期和时分改为固定分行显示，避免随卡片宽度产生不一致换行。
- 看板新增独立的搜索/归档工具行，任务类型和象限导航只保留真实项目及新增入口，并将 Tab 焦点循环扩展为四个区域。

## [2.7.1] - 2026-07-17

- 精简清除数据说明文案，并按当前多任务类型、归档、搜索与 Markdown 笔记同步能力更新插件说明。

## [2.7.0] - 2026-07-17

- 笔记同步设置新增一次性的“强制刷新同步”，确认后保存当前看板并重建全部任务类型及归档笔记；失败会明确提示且保留可重试状态。
- 修复旧同步内容残留在自动管理笔记顶部、造成看板与备份内容不一致的问题；原笔记会先安全移入“历史同步内容”，主笔记再生成当前看板的完整镜像。
- 看板宽度改为可使用 Obsidian 原生分栏拖动，桌面端最小宽度为 450px；同步文件夹候选框与输入框保持同宽。

## [2.6.1] - 2026-07-17

- 同步文件夹建议仅展示 Vault 第一层目录，并继续支持中英文输入实时过滤。
- 精简数据管理文案，移除同步文件夹下方的自动管理提示。

## [2.6.0] - 2026-07-16

- 同步文件夹设置新增 Vault 已有文件夹建议，聚焦时列出可选目录，输入中英文或路径片段时实时过滤，选择后自动保存并同步笔记。

## [2.5.0] - 2026-07-16

- 将笔记同步收敛为单一自动管理模式：统一目录下按任务类型创建独立 Markdown，任务类型增删改会自动创建、改名或移入恢复目录，并安全迁移旧双模式配置。

## [2.4.0] - 2026-07-16

- 新增推荐的单一汇总笔记同步模式，按任务类型和象限汇总进行中与归档任务，以稳定 ID 适配重命名，并兼容迁移现有分文件同步配置。
- 修正设置页数据管理与笔记同步标题的中文本地化，并精简清除数据按钮和不可撤销提示文案。
- 统一任务类型、任务列表和象限区域新增按钮的默认、悬停及聚焦样式，并将新增任务输入框改为单行起始、随换行自动增高。
- 将任务卡归档操作由下箭头改为与顶部归档入口一致的文件夹图标，并在悬停时使用归档橙色语义。
- 将对外产品名和新发布产物前缀改为 `aulycKanban`，保留插件 ID、安装目录、数据、视图和 CSS/DOM 兼容身份。

## [2.3.5] - 2026-07-16

- 移除普通任务列表的整体黑色底板，并为每张普通及归档任务卡使用独立主题背景色。

## [2.3.4] - 2026-07-15

- 归档列表空白区域仅在键盘可见焦点下显示整区焦点框，鼠标点击不再出现大白框。

## [2.3.3] - 2026-07-15

- 统一普通任务与归档模式中搜索、操作和列表组件的左右边界与内容宽度。

## [2.3.2] - 2026-07-15

- 将桌面端看板面板的固定宽度从 500px 调整为 600px。

## [2.3.1] - 2026-07-15

- 修复“全部任务”固定按钮的图标被通用 Tab 内边距压缩为零宽的问题。

## [2.3.0] - 2026-07-15

- 增加固定“全部任务”和“全部象限”聚合入口，并按两者交叉范围展示任务。
- 将任务快速输入改为共享搜索标签与折叠式新增入口，归档复用同一搜索和象限筛选。
- 保持三分区 Tab / Shift+Tab 与方向键导航，并补齐聚合视图的来源安全编辑、归档和同步。

## [2.2.1] - 2026-07-15

- 修复归档视图未聚焦时仍显示橙色边框的问题。

## [2.2.0] - 2026-07-15

- 简化归档选择操作，统一选择状态和批量删除交互。
- 接入 `obsidian-plugin` 通用发版规范、唯一权威版本源和独立递增构建号。
- 增加精确标签隔离构建、版本化 ZIP、release provenance、正式安装和真实加载验证门禁。
- 接入 Prettier 与 ESLint 质量门禁，并升级到 ESLint 9 flat config。
- 修复开发依赖安全公告，最低阈值 `npm audit` 恢复为零漏洞。
