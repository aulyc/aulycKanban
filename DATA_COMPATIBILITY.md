# 数据兼容与备份格式

本文说明插件运行数据、可移植备份和 Store 状态边界。产品版本、发布版本与构建号仍以
`release-version.json` 为准，不与这里的数据格式版本混用。

## `data.json` schema

Obsidian 将插件运行数据保存在 `.obsidian/plugins/aulyckanban/data.json`。当前
`settings.schemaVersion` 为 `8`，只在持久化结构发生兼容性变化时递增。

加载规则：

- 首次运行或没有数据时创建默认看板。
- 缺少 `schemaVersion` 的历史数据视为 legacy 数据；版本 `0` 至 `8` 继续通过结构探测、
  清洗和迁移加载，内存中的结果统一标记为版本 `8`。
- 显式版本不是非负整数时停止加载，避免损坏数据被默认值覆盖。
- 显式版本高于 `8` 时停止加载并要求升级插件，当前插件不会保存或覆盖该数据。

历史迁移继续支持单 `columns`、固定 `work`/`personal` 和动态 `views`/`archives`
结构。迁移以真实结构为依据，不虚构不存在的逐版本转换步骤。

## JSON 备份

备份只包含看板任务数据，不包含同步目录、界面语言等插件设置。当前导出格式版本为
`4.0`。

| 声明版本               | 结构                                       | 导入规则                   |
| ---------------------- | ------------------------------------------ | -------------------------- |
| 无版本                 | 单 `columns`、固定双任务类型或动态任务类型 | 作为历史备份严格校验后迁移 |
| `2.0`                  | `work`、`personal` 及可选归档              | 迁移到动态任务类型         |
| `4.0`                  | `views`、`archives`                        | 按当前格式导入             |
| 高于 `4.0`             | 任意                                       | 拒绝并提示升级插件         |
| 其他或版本与结构不匹配 | 任意                                       | 拒绝，不猜测格式           |

所有导入在替换当前看板前完成字段、重复 ID、版本和结构校验。失败时不 dispatch、不保存，
不会产生部分导入。

## Store 状态所有权

`KanbanStore` 在接收设置或看板数据时取得深拷贝，调用方之后修改原对象不会影响 Store。
`getSettings()` 和 `getBoardData()` 返回递归只读的防御性快照；业务修改必须通过
`dispatch()`，普通设置更新不能修改 `schemaVersion`。

为避免大看板反复深拷贝影响渲染，高频 UI 查询继续使用专用查询接口，并由插件内部按
只读方式消费；完整深拷贝只在持久化、导出或显式读取根状态时发生。
